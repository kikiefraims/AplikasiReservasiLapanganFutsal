<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.pringsewufutsal.pringsewufutsalreservasi">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_intermilan"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".TambahTransaksi" />
        <activity android:name=".DetailTransaksi" />
        <activity android:name=".EditTransaksi" />
        <activity android:name=".TambahPetugas" />
    </application>

</manifest>